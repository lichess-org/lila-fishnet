name: Test

on: [push, pull_request]

jobs:
  openjdk21:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - uses: actions/setup-java@v3
      with:
        java-version: 21
    - run: sbt compile
